space:
  spaceName:
    testSpace

spring:
  data:
    mongodb:
#      uri: ${MONGODB_URI}
      uri: "mongodb://localhost/easydb"
  application:
    name: easydb
  metrics:
    prometheus:
      enabled: true

management:
  security:
    enabled: false

  endpoints:
    web:
      exposure:
        include: '*'